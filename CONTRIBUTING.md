# 贡献指南

欢迎新增、修订和归类提示词。提交内容应当可复用、来源清楚，并避免简单搬运第三方条目。

## 条目格式

````markdown
<a id="case-xx"></a>
### 案例 XX：标题

**标签：** `标签1` `标签2`　**画幅：** `比例`

```text
完整提示词
```
````

## 内容要求

- 一条提示词应包含目标、主体、构图、风格、光色、文字、约束和输出规格中的必要部分。
- 方括号表示变量，如 `[产品名]`；固定显示的文字使用中文引号。
- 涉及数据、科普或历史时，注明“使用用户提供并核验的数据”，不要让模型自由编造。
- 涉及参考图时，列清不可改变的身份、产品或角色特征。
- 不提交违法、欺诈、冒充、仇恨、色情、侵权或明显误导的提示词。

## 来源与授权

- 原创条目标记“原创”。
- 改编条目注明原作者、原始链接、许可证和改动说明。
- 未知来源或无法确认授权的完整提示词不收录；可以只总结方法并重新创作。
- 图片必须具有可再分发授权，或由贡献者自行生成并明确模型与日期。

## 编号与索引

新增案例时同时更新：

1. `docs/gallery.md` 的导航表。
2. 对应 `gallery-part-*.md` 文件。
3. `data/cases.json` 的机器索引。
4. README 中的案例数量徽章。
